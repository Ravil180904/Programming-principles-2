#1. Write a function that takes a single movie and returns True if its IMDB score is above 5.5

def ppp(movies_movie):
    for m in movies:
        if ["IMBD"] > 5.5:
            return True
        elif["IMBD"] < 5.5:
            return False
        return "IMBD"
print ppp(movies_movie)